---
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Boston Web3 Meetup at Fenway Park</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;600;700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
  <style is:inline>
    :root {
      --navy: #0C2340;
      --red: #BD3039;
      --gold: #C8A951;
      --white: #F5F5F5;
      --card-bg: #FFFFFF;
      --text: #1a1a1a;
      --text-light: #555;
    }

    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Open Sans', system-ui, sans-serif;
      color: var(--text);
      background: var(--white);
      line-height: 1.6;
    }

    h1, h2, h3, h4 {
      font-family: 'Oswald', system-ui, sans-serif;
      font-weight: 700;
      line-height: 1.2;
    }

    /* Hero */
    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background:
        linear-gradient(135deg, rgba(12,35,64,0.88) 0%, rgba(26,58,92,0.82) 40%, rgba(189,48,57,0.75) 100%),
        url('https://cdn.prod.website-files.com/64aa957da446d8dc44869f81/69b8d937e2a5ef57890eeff3_Pavilion%20Hospitality%20Seat%20View.jpg') center/cover no-repeat;
      color: #fff;
      padding: 2rem;
      position: relative;
    }

    .hero-inner {
      display: flex;
      align-items: center;
      gap: clamp(2rem, 5vw, 4rem);
      max-width: 1100px;
      width: 100%;
      margin: 0 auto;
    }

    .hero-logo {
      width: clamp(200px, 28vw, 360px);
      height: auto;
      flex-shrink: 0;
      filter: drop-shadow(0 6px 20px rgba(0,0,0,0.4));
    }

    .hero-text {
      text-align: left;
    }

    .hero h1 {
      font-size: clamp(2.2rem, 5vw, 4rem);
      text-transform: uppercase;
      letter-spacing: 0.05em;
      margin-bottom: 0.5rem;
      text-shadow: 2px 3px 6px rgba(0,0,0,0.4);
    }

    .hero .subtitle {
      font-size: clamp(1rem, 2.2vw, 1.3rem);
      opacity: 0.9;
      margin-bottom: 0.5rem;
      font-weight: 600;
    }

    .hero .experience-note {
      font-size: clamp(0.9rem, 1.8vw, 1.05rem);
      opacity: 0.85;
      margin-bottom: 0.75rem;
      line-height: 1.5;
    }

    .hero .date {
      font-family: 'Oswald', sans-serif;
      font-size: clamp(1.1rem, 2.5vw, 1.5rem);
      color: var(--gold);
      margin-bottom: 1.5rem;
    }

    .hero .scroll-btn {
      display: inline-block;
      padding: 0.7rem 2rem;
      background: transparent;
      border: 2px solid rgba(255,255,255,0.5);
      color: #fff;
      font-family: 'Oswald', sans-serif;
      font-size: 1rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      border-radius: 4px;
      cursor: pointer;
      transition: background 0.2s, border-color 0.2s;
    }

    .hero .scroll-btn:hover {
      background: rgba(255,255,255,0.15);
      border-color: #fff;
    }

    /* Interest CTA Section */
    .interest-cta {
      background: var(--navy);
      padding: 4rem 1.5rem;
      text-align: center;
    }

    .interest-cta-inner {
      max-width: 900px;
      margin: 0 auto;
    }

    .interest-cta h2 {
      font-size: clamp(1.8rem, 4vw, 2.5rem);
      color: #fff;
      text-transform: uppercase;
      margin-bottom: 0.5rem;
    }

    .interest-cta > .interest-cta-inner > p {
      color: rgba(255,255,255,0.8);
      font-size: 1.05rem;
      margin-bottom: 2rem;
    }

    .fenway-cta-wrapper {
      display: inline-block;
      border: 4px solid var(--gold);
      border-radius: 12px;
      padding: 6px;
      box-shadow: 0 0 30px rgba(200,169,81,0.3);
      margin-bottom: 2rem;
      transition: box-shadow 0.3s, transform 0.3s;
      cursor: pointer;
    }

    .fenway-cta-wrapper:hover {
      box-shadow: 0 0 45px rgba(200,169,81,0.5);
      transform: translateY(-3px);
    }

    .fenway-cta-img {
      display: block;
      width: 100%;
      max-width: 700px;
      border-radius: 8px;
    }

    .fenway-cta-label {
      display: block;
      font-family: 'Oswald', sans-serif;
      font-size: 1.1rem;
      color: var(--gold);
      text-transform: uppercase;
      letter-spacing: 0.05em;
      margin-top: 0.5rem;
      padding-bottom: 0.25rem;
    }

    /* Interest Form */
    .interest-form {
      max-width: 600px;
      margin: 0 auto;
      text-align: left;
    }

    .interest-form h3 {
      font-size: 1.4rem;
      color: var(--gold);
      text-transform: uppercase;
      margin-bottom: 1.5rem;
      text-align: center;
    }

    .form-group {
      margin-bottom: 1.25rem;
    }

    .form-group label {
      display: block;
      font-family: 'Oswald', sans-serif;
      font-size: 0.95rem;
      color: #fff;
      text-transform: uppercase;
      letter-spacing: 0.03em;
      margin-bottom: 0.4rem;
    }

    .form-group input,
    .form-group select {
      width: 100%;
      padding: 0.65rem 0.9rem;
      border: 2px solid rgba(255,255,255,0.2);
      border-radius: 6px;
      background: rgba(255,255,255,0.1);
      color: #fff;
      font-family: 'Open Sans', sans-serif;
      font-size: 0.95rem;
      transition: border-color 0.2s;
    }

    .form-group input::placeholder {
      color: rgba(255,255,255,0.5);
    }

    .form-group input:focus,
    .form-group select:focus {
      outline: none;
      border-color: var(--gold);
    }

    .form-group select {
      cursor: pointer;
    }

    .form-group select option {
      background: var(--navy);
      color: #fff;
    }

    .form-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
    }

    .form-submit {
      display: block;
      width: 100%;
      padding: 0.85rem;
      background: var(--gold);
      color: var(--navy);
      font-family: 'Oswald', sans-serif;
      font-size: 1.15rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      margin-top: 0.5rem;
      transition: background 0.2s, transform 0.2s;
    }

    .form-submit:hover {
      background: #d4b85c;
      transform: translateY(-2px);
    }

    .form-success {
      display: none;
      text-align: center;
      padding: 2rem;
      color: var(--gold);
      font-family: 'Oswald', sans-serif;
      font-size: 1.3rem;
    }

    /* Stitch divider */
    .stitch-divider {
      width: 100%;
      overflow: hidden;
      line-height: 0;
      background: var(--white);
    }

    .stitch-divider svg {
      display: block;
      width: 100%;
      height: 40px;
    }

    /* Game Series */
    .series {
      max-width: 1100px;
      margin: 0 auto;
      padding: 4rem 1.5rem 2rem;
    }

    .series > h2 {
      text-align: center;
      font-size: clamp(1.8rem, 4vw, 2.5rem);
      color: var(--navy);
      text-transform: uppercase;
      margin-bottom: 0.5rem;
    }

    .series > p {
      text-align: center;
      color: var(--text-light);
      margin-bottom: 2.5rem;
      font-size: 1.05rem;
    }

    .series-cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
    }

    .series-card {
      background: var(--card-bg);
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.08);
      overflow: hidden;
      text-align: center;
      transition: transform 0.25s, box-shadow 0.25s;
    }

    .series-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 30px rgba(0,0,0,0.12);
    }

    .series-card-header {
      background: var(--navy);
      color: #fff;
      padding: 1.25rem;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 1rem;
    }

    .series-card-header img {
      width: 48px;
      height: 48px;
      object-fit: contain;
    }

    .series-card-header h3 {
      font-size: 1.2rem;
      text-transform: uppercase;
    }

    .series-card-body {
      padding: 1.5rem;
    }

    .series-dates {
      font-family: 'Oswald', sans-serif;
      font-size: 1.3rem;
      color: var(--red);
      margin-bottom: 0.5rem;
    }

    .series-opponent {
      font-size: 1rem;
      color: var(--text-light);
    }

    .series-badge {
      display: inline-block;
      background: var(--gold);
      color: var(--navy);
      font-family: 'Oswald', sans-serif;
      font-weight: 700;
      font-size: 0.85rem;
      padding: 0.3rem 0.8rem;
      border-radius: 3px;
      text-transform: uppercase;
      margin-top: 0.75rem;
    }

    .game-schedule {
      margin-top: 1rem;
      text-align: left;
    }

    .game-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0.5rem 0.75rem;
      border-bottom: 1px solid #eee;
      font-size: 0.9rem;
    }

    .game-row:last-child {
      border-bottom: none;
    }

    .game-day {
      font-weight: 600;
      color: var(--navy);
      min-width: 90px;
    }

    .game-time {
      font-family: 'Oswald', sans-serif;
      color: var(--red);
      font-weight: 600;
    }

    .game-promo {
      font-size: 0.75rem;
      color: var(--gold);
      background: var(--navy);
      padding: 0.15rem 0.5rem;
      border-radius: 3px;
      font-weight: 600;
    }

    /* Packages */
    .packages {
      max-width: 1100px;
      margin: 0 auto;
      padding: 3rem 1.5rem 4rem;
    }

    .packages > h2 {
      text-align: center;
      font-size: clamp(1.8rem, 4vw, 2.5rem);
      color: var(--navy);
      text-transform: uppercase;
      margin-bottom: 0.5rem;
    }

    .packages > p {
      text-align: center;
      color: var(--text-light);
      margin-bottom: 3rem;
      font-size: 1.05rem;
    }

    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
      gap: 2rem;
    }

    .card {
      background: var(--card-bg);
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.08);
      overflow: hidden;
      transition: transform 0.25s, box-shadow 0.25s;
    }

    .card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 30px rgba(0,0,0,0.12);
    }

    .card-header {
      background: var(--navy);
      color: #fff;
      padding: 1.25rem 1.5rem;
    }

    .card-header h3 {
      font-size: 1.3rem;
      text-transform: uppercase;
      letter-spacing: 0.03em;
    }

    .card-body {
      padding: 1.5rem;
    }

    .price-badge {
      display: inline-block;
      background: var(--red);
      color: #fff;
      font-family: 'Oswald', sans-serif;
      font-size: 1.2rem;
      font-weight: 700;
      padding: 0.5rem 1.2rem;
      border-radius: 4px;
      margin-bottom: 1rem;
    }

    .price-badge .per-person {
      font-size: 0.85rem;
      font-weight: 400;
      opacity: 0.9;
      display: block;
    }

    .features {
      list-style: none;
      margin-bottom: 1.25rem;
    }

    .features li {
      padding: 0.4rem 0 0.4rem 1.5rem;
      position: relative;
      border-left: 3px solid var(--red);
      margin-bottom: 0.5rem;
      margin-left: 0.25rem;
      font-size: 0.95rem;
    }

    .features li::before {
      content: "\2713";
      position: absolute;
      left: 0.35rem;
      color: var(--red);
      font-weight: 700;
    }

    /* Slider */
    .slider-section {
      margin: 1.25rem 0;
      padding: 1rem;
      background: #f7f7f7;
      border-radius: 8px;
    }

    .slider-section label {
      display: block;
      font-family: 'Oswald', sans-serif;
      font-size: 1rem;
      color: var(--navy);
      text-transform: uppercase;
      margin-bottom: 0.5rem;
    }

    .slider-row {
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .slider-row input[type="range"] {
      flex: 1;
      accent-color: var(--red);
      height: 6px;
      cursor: pointer;
    }

    .slider-value {
      font-family: 'Oswald', sans-serif;
      font-size: 1.3rem;
      font-weight: 700;
      color: var(--red);
      min-width: 2.5ch;
      text-align: center;
    }

    .slider-total {
      margin-top: 0.5rem;
      font-size: 0.95rem;
      color: var(--text-light);
    }

    .slider-total strong {
      color: var(--navy);
      font-family: 'Oswald', sans-serif;
      font-size: 1.1rem;
    }

    /* Cantina layout explainer */
    .cantina-layout {
      margin: 1rem 0;
      padding: 1rem;
      background: #f7f7f7;
      border-radius: 8px;
    }

    .cantina-layout h4 {
      font-size: 1rem;
      color: var(--navy);
      text-transform: uppercase;
      margin-bottom: 0.75rem;
    }

    .cantina-zones {
      display: flex;
      gap: 0.75rem;
      margin-bottom: 0.75rem;
    }

    .cantina-zone {
      flex: 1;
      background: var(--navy);
      color: #fff;
      border-radius: 6px;
      padding: 0.75rem;
      text-align: center;
      font-size: 0.85rem;
    }

    .cantina-zone strong {
      display: block;
      font-family: 'Oswald', sans-serif;
      font-size: 1rem;
      margin-bottom: 0.25rem;
    }

    .cantina-zone .zone-price {
      color: var(--gold);
      font-family: 'Oswald', sans-serif;
      font-weight: 700;
    }

    .cantina-note {
      font-size: 0.85rem;
      color: var(--text-light);
      font-style: italic;
    }

    /* Toggle sections */
    .toggle-btn {
      display: inline-block;
      background: none;
      border: 2px solid var(--navy);
      color: var(--navy);
      font-family: 'Oswald', sans-serif;
      font-size: 0.95rem;
      font-weight: 600;
      padding: 0.45rem 1rem;
      border-radius: 4px;
      cursor: pointer;
      text-transform: uppercase;
      letter-spacing: 0.03em;
      transition: background 0.2s, color 0.2s;
      margin-right: 0.5rem;
      margin-bottom: 0.5rem;
    }

    .toggle-btn:hover {
      background: var(--navy);
      color: #fff;
    }

    .toggle-btn.active {
      background: var(--navy);
      color: #fff;
    }

    .addons-content {
      max-height: 0;
      overflow: hidden;
      transition: max-height 0.4s ease;
    }

    .addons-content.visible {
      max-height: 500px;
    }

    .addons-content .inner {
      padding: 1rem 0;
      border-top: 1px solid #eee;
      margin-top: 0.75rem;
    }

    .addons-content ul {
      list-style: none;
      padding: 0;
    }

    .addons-content li {
      padding: 0.3rem 0 0.3rem 1.2rem;
      position: relative;
      color: var(--text-light);
      font-size: 0.9rem;
    }

    .addons-content li::before {
      content: "+";
      position: absolute;
      left: 0;
      color: var(--gold);
      font-weight: 700;
      font-size: 1.1rem;
    }

    /* Virtual tour */
    .tour-container {
      margin-top: 1rem;
    }

    .tour-frame {
      display: none;
      margin-top: 0.75rem;
    }

    .tour-frame.visible {
      display: block;
    }

    .tour-frame .iframe-wrapper {
      position: relative;
      width: 100%;
      aspect-ratio: 16 / 9;
      border-radius: 6px;
      overflow: hidden;
      background: #ddd;
    }

    .tour-frame iframe {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      border: none;
    }

    .tour-frame .fallback {
      display: block;
      margin-top: 0.5rem;
      font-size: 0.85rem;
      color: var(--text-light);
    }

    .tour-frame .fallback a {
      color: var(--red);
    }

    /* Comparison table */
    .comparison {
      max-width: 900px;
      margin: 0 auto;
      padding: 0 1.5rem 4rem;
    }

    .comparison h2 {
      text-align: center;
      font-size: clamp(1.5rem, 3vw, 2rem);
      color: var(--navy);
      text-transform: uppercase;
      margin-bottom: 1.5rem;
    }

    .comparison table {
      width: 100%;
      border-collapse: collapse;
      font-size: 0.95rem;
    }

    .comparison thead {
      background: var(--navy);
      color: #fff;
    }

    .comparison th {
      font-family: 'Oswald', sans-serif;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.03em;
      padding: 0.75rem 1rem;
      text-align: left;
    }

    .comparison td {
      padding: 0.65rem 1rem;
      border-bottom: 1px solid #e0e0e0;
    }

    .comparison tbody tr:nth-child(even) {
      background: #f9f9f9;
    }

    .comparison tbody tr:hover {
      background: #f0f0f0;
    }

    /* Event Planners */
    .planners {
      max-width: 900px;
      margin: 0 auto;
      padding: 3rem 1.5rem 4rem;
      text-align: center;
    }

    .planners h2 {
      font-size: clamp(1.8rem, 4vw, 2.5rem);
      color: var(--navy);
      text-transform: uppercase;
      margin-bottom: 0.5rem;
    }

    .planners > p {
      color: var(--text-light);
      margin-bottom: 2.5rem;
      font-size: 1.05rem;
    }

    .planners-grid {
      display: flex;
      justify-content: center;
      gap: 3rem;
      flex-wrap: wrap;
    }

    .planner {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-decoration: none;
      color: var(--text);
      transition: transform 0.2s;
    }

    .planner:hover {
      transform: translateY(-4px);
    }

    .planner-img {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      object-fit: cover;
      border: 4px solid var(--navy);
      box-shadow: 0 4px 15px rgba(0,0,0,0.15);
      transition: border-color 0.2s;
    }

    .planner:hover .planner-img {
      border-color: var(--red);
    }

    .planner-name {
      font-family: 'Oswald', sans-serif;
      font-size: 1.1rem;
      font-weight: 600;
      margin-top: 0.75rem;
      color: var(--navy);
    }

    .planner-handle {
      font-size: 0.85rem;
      color: var(--text-light);
      margin-top: 0.15rem;
    }

    /* Footer */
    footer {
      background: var(--navy);
      color: #ccc;
      text-align: center;
      padding: 2rem 1.5rem;
      font-size: 0.9rem;
    }

    footer strong {
      color: #fff;
      font-family: 'Oswald', sans-serif;
      font-size: 1.1rem;
      display: block;
      margin-bottom: 0.25rem;
    }

    footer a {
      color: var(--gold);
      text-decoration: none;
    }

    footer a:hover {
      text-decoration: underline;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .hero-inner {
        flex-direction: column;
        text-align: center;
      }

      .hero-text {
        text-align: center;
      }

      .hero-logo {
        width: clamp(180px, 50vw, 280px);
      }

      .cards {
        grid-template-columns: 1fr;
      }

      .series-cards {
        grid-template-columns: 1fr;
      }

      .comparison table {
        font-size: 0.85rem;
      }

      .comparison th, .comparison td {
        padding: 0.5rem 0.6rem;
      }

      .cantina-zones {
        flex-direction: column;
      }

      .planners-grid {
        gap: 2rem;
      }

      .planner-img {
        width: 100px;
        height: 100px;
      }

      .form-row {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>

  <!-- Hero -->
  <section class="hero">
    <div class="hero-inner">
      <img src="https://cdn.prod.website-files.com/64aa957da446d8dc44869f81/69b78fe2b73770a04d4576e6_27f15ca4-9bfa-4224-8dea-c2884004a2ca.png" alt="Boston Web3 Logo" class="hero-logo" />
      <div class="hero-text">
        <h1>Boston Web3 Meetup<br>at Fenway Park</h1>
        <p class="subtitle">More than a game &mdash; it's a full Boston Web3 experience</p>
        <p class="experience-note">We're putting together a bigger experience: meet fellow Web3 diehards IRL, explore an art gallery, do a historical walk through Boston, and more. Details will come as we figure out who's interested &mdash; we want to make this as affordable as possible for our community.</p>
        <p class="date">September 2026 &mdash; Fenway Park, Boston</p>
        <button class="scroll-btn" onclick="document.getElementById('series').scrollIntoView({behavior:'smooth'})">
          See the Games &#9660;
        </button>
      </div>
    </div>
  </section>

  <!-- Stitch divider -->
  <div class="stitch-divider">
    <svg viewBox="0 0 1200 40" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M0,40 Q300,0 600,20 T1200,0 L1200,40 Z" fill="#0C2340"/>
      <path d="M0,20 Q150,5 300,18 Q450,32 600,18 Q750,5 900,18 Q1050,32 1200,18" fill="none" stroke="#BD3039" stroke-width="2.5" stroke-dasharray="8,6"/>
    </svg>
  </div>

  <!-- Game Series -->
  <section class="series" id="series">
    <h2>The Games</h2>
    <p>Two potential series we're targeting — vote for your favorite!</p>

    <div class="series-cards">

      <!-- Series 1: Royals -->
      <div class="series-card">
        <div class="series-card-header">
          <img src="https://a.espncdn.com/i/teamlogos/mlb/500/kc.png" alt="Kansas City Royals Logo" />
          <h3>Red Sox vs. Royals</h3>
        </div>
        <div class="series-card-body">
          <div class="series-dates">September 11 &ndash; 13, 2026</div>
          <div class="series-opponent">3-game series at Fenway Park</div>
          <div class="game-schedule">
            <div class="game-row">
              <span class="game-day">Fri, Sep 11</span>
              <span class="game-time">7:10 PM ET</span>
            </div>
            <div class="game-row">
              <span class="game-day">Sat, Sep 12</span>
              <span class="game-time">4:10 PM ET</span>
              <span class="game-promo">Bruins Day</span>
            </div>
            <div class="game-row">
              <span class="game-day">Sun, Sep 13</span>
              <span class="game-time">1:35 PM ET</span>
            </div>
          </div>
          <div class="series-badge">Option A</div>
        </div>
      </div>

      <!-- Series 2: Cubs -->
      <div class="series-card">
        <div class="series-card-header">
          <img src="https://a.espncdn.com/i/teamlogos/mlb/500/chc.png" alt="Chicago Cubs Logo" />
          <h3>Red Sox vs. Cubs</h3>
        </div>
        <div class="series-card-body">
          <div class="series-dates">September 25 &ndash; 27, 2026</div>
          <div class="series-opponent">3-game series at Fenway Park</div>
          <div class="game-schedule">
            <div class="game-row">
              <span class="game-day">Fri, Sep 25</span>
              <span class="game-time">7:10 PM ET</span>
              <span class="game-promo">Pok&eacute;mon GO Night</span>
            </div>
            <div class="game-row">
              <span class="game-day">Sat, Sep 26</span>
              <span class="game-time">7:15 PM ET</span>
            </div>
            <div class="game-row">
              <span class="game-day">Sun, Sep 27</span>
              <span class="game-time">3:05 PM ET</span>
            </div>
          </div>
          <div class="series-badge">Option B</div>
        </div>
      </div>

    </div>
  </section>

  <!-- Packages -->
  <section class="packages" id="packages">
    <h2>Reception Packages</h2>
    <p>Two incredible options for your Fenway Park experience</p>

    <div class="cards">

      <!-- Card 1: Carl's Corner -->
      <div class="card">
        <div class="card-header">
          <h3>Carl's Corner Pre-Game Reception</h3>
        </div>
        <div class="card-body">
          <div class="price-badge" id="carls-price-badge">
            $4,275 for 15 Guests
            <span class="per-person">$285 per person</span>
          </div>

          <!-- Guest Slider -->
          <div class="slider-section">
            <label for="carls-slider">Choose Your Group Size</label>
            <div class="slider-row">
              <span style="font-size:0.85rem;color:var(--text-light)">15</span>
              <input type="range" id="carls-slider" min="15" max="25" value="15" />
              <span style="font-size:0.85rem;color:var(--text-light)">25</span>
              <span class="slider-value" id="carls-count">15</span>
            </div>
            <div class="slider-total" id="carls-total">
              Total: <strong>$4,275</strong> for 15 guests at $285/person
            </div>
          </div>

          <ul class="features">
            <li>2-hour pre-game reception in Carl's Corner space</li>
            <li>All-you-can-eat New England buffet for the full 2 hours</li>
            <li>Hosted bar &mdash; beer, wine, soda, water, seltzers (2 hrs)</li>
            <li>Dedicated server for your group</li>
            <li>Seats together in Pavilion Reserved 18 or 20, right next to your reception space</li>
          </ul>

          <button class="toggle-btn" data-target="addons-carls">Add-ons &amp; Details</button>
          <button class="toggle-btn" data-tour="tour-carls">Virtual Tour</button>

          <div class="addons-content" id="addons-carls">
            <div class="inner">
              <ul>
                <li>Up to 25 total guests — use the slider above to adjust</li>
                <li>Bar can be extended for an additional 2 hours at $40/person</li>
              </ul>
            </div>
          </div>

          <div class="tour-container">
            <div class="tour-frame" id="tour-carls" data-src="https://suiteview.com/tour/redsox-group/?s=pano49152">
              <div class="iframe-wrapper"></div>
              <span class="fallback">Can't see the tour? <a href="https://suiteview.com/tour/redsox-group/?s=pano49152" target="_blank" rel="noopener">Open in new tab &rarr;</a></span>
            </div>
          </div>
        </div>
      </div>

      <!-- Card 2: Hornitos Cantina -->
      <div class="card">
        <div class="card-header">
          <h3>Hornitos Cantina In-Game Reception</h3>
        </div>
        <div class="card-body">
          <div class="price-badge">
            From $4,680
            <span class="per-person">Starting at 24 guests</span>
          </div>

          <ul class="features">
            <li>Semi-private area with barstool &amp; tabletop seating</li>
            <li>Full access to the Cantina throughout the entire game</li>
            <li>TVs, drink rails, and tables for your group</li>
            <li>2-hour Ballpark favorites buffet starting at first pitch</li>
            <li>2-hour hosted bar &mdash; beer, wine, soda, water, seltzers</li>
          </ul>

          <!-- Cantina Layout Explainer -->
          <div class="cantina-layout">
            <h4>Flexible Booking — Two Separable Areas</h4>
            <p style="font-size:0.9rem;color:var(--text-light);margin-bottom:0.75rem;">
              The Cantina is divided into <strong>two distinct zones</strong> that can be booked independently or combined for a larger group.
            </p>
            <div class="cantina-zones">
              <div class="cantina-zone">
                <strong>Zone A</strong>
                24 guests<br>
                <span class="zone-price">$4,680</span><br>
                <span style="font-size:0.8rem;opacity:0.85">~$195/person</span>
              </div>
              <div class="cantina-zone">
                <strong>Zone B</strong>
                42 guests<br>
                <span class="zone-price">$8,190</span><br>
                <span style="font-size:0.8rem;opacity:0.85">~$195/person</span>
              </div>
            </div>
            <p class="cantina-note">
              Book both zones together for the full Cantina: 66 guests at $12,870. Each zone has its own bar &amp; buffet service. If only one zone is booked, the other may host a separate group.
            </p>
          </div>

          <button class="toggle-btn" data-target="addons-cantina">Add-ons &amp; Details</button>
          <button class="toggle-btn" data-tour="tour-cantina">Virtual Tour</button>

          <div class="addons-content" id="addons-cantina">
            <div class="inner">
              <ul>
                <li>Bar extension: additional 2 hours for $25/person</li>
                <li>Zone A (24 guests) or Zone B (42 guests) can be booked separately</li>
                <li>Both zones combined: 66 guests for the full Cantina experience</li>
                <li>Each zone's bar &amp; buffet are exclusive to your party</li>
              </ul>
            </div>
          </div>

          <div class="tour-container">
            <div class="tour-frame" id="tour-cantina" data-src="https://suiteview.com/tour/redsox-group/?s=pano49187">
              <div class="iframe-wrapper"></div>
              <span class="fallback">Can't see the tour? <a href="https://suiteview.com/tour/redsox-group/?s=pano49187" target="_blank" rel="noopener">Open in new tab &rarr;</a></span>
            </div>
          </div>
        </div>
      </div>

    </div>
  </section>

  <!-- Comparison Table -->
  <section class="comparison">
    <h2>Quick Comparison</h2>
    <table>
      <thead>
        <tr>
          <th></th>
          <th>Carl's Corner</th>
          <th>Hornitos Cantina</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><strong>Guests</strong></td>
          <td>15&ndash;25</td>
          <td>24 / 42 / 66</td>
        </tr>
        <tr>
          <td><strong>Timing</strong></td>
          <td>Pre-game (2 hrs before first pitch)</td>
          <td>In-game (starts at first pitch)</td>
        </tr>
        <tr>
          <td><strong>Per Person</strong></td>
          <td>$285</td>
          <td>~$195</td>
        </tr>
        <tr>
          <td><strong>Food</strong></td>
          <td>New England buffet</td>
          <td>Ballpark favorites buffet</td>
        </tr>
        <tr>
          <td><strong>Seating</strong></td>
          <td>Pavilion Reserved 18/20</td>
          <td>Barstool &amp; tabletop (in-space)</td>
        </tr>
        <tr>
          <td><strong>Bar Extension</strong></td>
          <td>+2 hrs @ $40/person</td>
          <td>+2 hrs @ $25/person</td>
        </tr>
        <tr>
          <td><strong>Layout</strong></td>
          <td>Single private space</td>
          <td>2 zones (bookable separately)</td>
        </tr>
        <tr>
          <td><strong>Total (Base)</strong></td>
          <td>$4,275</td>
          <td>$4,680 / $8,190 / $12,870</td>
        </tr>
      </tbody>
    </table>
  </section>

  <!-- Interest CTA with Fenway Image & Form -->
  <section class="interest-cta" id="interest">
    <div class="interest-cta-inner">
      <h2>Interested? Let Us Know</h2>
      <p>We're gauging interest to lock in the best deal for our Web3 community. Drop your info below and we'll keep you in the loop.</p>

      <a href="#interest-form" class="fenway-cta-wrapper" onclick="event.preventDefault(); document.getElementById('interest-form').scrollIntoView({behavior:'smooth'})">
        <img src="https://cdn.prod.website-files.com/64aa957da446d8dc44869f81/69b8d937e2a5ef57890eeff3_Pavilion%20Hospitality%20Seat%20View.jpg" alt="Fenway Park Pavilion Hospitality View" class="fenway-cta-img" />
        <span class="fenway-cta-label">Click here to fill out the interest form below</span>
      </a>

      <div class="interest-form" id="interest-form">
        <h3>Sign Up for Updates</h3>
        <form id="boston-web3-form" onsubmit="handleFormSubmit(event)">
          <div class="form-row">
            <div class="form-group">
              <label for="form-name">Your Name</label>
              <input type="text" id="form-name" name="name" placeholder="First & Last Name" required />
            </div>
            <div class="form-group">
              <label for="form-x">Your X (Twitter) Handle</label>
              <input type="text" id="form-x" name="x_handle" placeholder="@yourhandle" />
            </div>
          </div>
          <div class="form-row">
            <div class="form-group">
              <label for="form-game">Which Series?</label>
              <select id="form-game" name="game">
                <option value="">Select a series...</option>
                <option value="royals">Option A: Red Sox vs. Royals (Sep 11-13)</option>
                <option value="cubs">Option B: Red Sox vs. Cubs (Sep 25-27)</option>
                <option value="either">Either series works for me</option>
              </select>
            </div>
            <div class="form-group">
              <label for="form-date">Preferred Game Date</label>
              <select id="form-date" name="date">
                <option value="">Select a date...</option>
                <optgroup label="Royals Series (Option A)">
                  <option value="sep11">Fri, Sep 11 — 7:10 PM</option>
                  <option value="sep12">Sat, Sep 12 — 4:10 PM (Bruins Day)</option>
                  <option value="sep13">Sun, Sep 13 — 1:35 PM</option>
                </optgroup>
                <optgroup label="Cubs Series (Option B)">
                  <option value="sep25">Fri, Sep 25 — 7:10 PM (Pok&eacute;mon GO Night)</option>
                  <option value="sep26">Sat, Sep 26 — 7:15 PM</option>
                  <option value="sep27">Sun, Sep 27 — 3:05 PM</option>
                </optgroup>
                <option value="any">Any date works</option>
              </select>
            </div>
          </div>
          <div class="form-group">
            <label for="form-package">Preferred Package</label>
            <select id="form-package" name="package">
              <option value="">Select a package...</option>
              <option value="carls">Carl's Corner Pre-Game Reception ($285/person)</option>
              <option value="cantina">Hornitos Cantina In-Game Reception (~$195/person)</option>
              <option value="unsure">Not sure yet — tell me more</option>
            </select>
          </div>
          <button type="submit" class="form-submit">Count Me In</button>
        </form>
        <div class="form-success" id="form-success">
          Thanks for your interest! We'll be in touch as plans come together.
        </div>
      </div>
    </div>
  </section>

  <!-- Event Planners -->
  <section class="planners">
    <h2>Your Event Planners</h2>
    <p>Reach out to us on X with any questions</p>
    <div class="planners-grid">
      <a href="https://x.com/T1MLivingston" target="_blank" rel="noopener" class="planner">
        <img src="https://pbs.twimg.com/profile_images/2032683131690446848/VvyrQMyP_400x400.jpg" alt="Tim Livingston" class="planner-img" />
        <span class="planner-name">Tim Livingston</span>
        <span class="planner-handle">@T1MLivingston</span>
      </a>
      <a href="https://x.com/Teacher96_Katie" target="_blank" rel="noopener" class="planner">
        <img src="https://pbs.twimg.com/profile_images/2027809249284292608/8rHs7OcH_400x400.jpg" alt="Katie" class="planner-img" />
        <span class="planner-name">Katie</span>
        <span class="planner-handle">@Teacher96_Katie</span>
      </a>
      <a href="https://x.com/montanakind" target="_blank" rel="noopener" class="planner">
        <img src="https://cdn.prod.website-files.com/64aa957da446d8dc44869f81/694e9a4fbdd7184b657c0f77_n_17763%20Small.jpeg" alt="Luke" class="planner-img" />
        <span class="planner-name">Luke</span>
        <span class="planner-handle">@montanakind</span>
      </a>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <strong>Fenway Park &bull; Boston, MA</strong>
    <p>4 Jersey Street, Boston, MA 02215</p>
  </footer>

  <script is:inline>
    function handleFormSubmit(e) {
      e.preventDefault();
      var form = document.getElementById('boston-web3-form');
      var success = document.getElementById('form-success');
      form.style.display = 'none';
      success.style.display = 'block';
    }

    document.addEventListener('DOMContentLoaded', function () {
      // Carl's Corner guest slider
      var slider = document.getElementById('carls-slider');
      var countEl = document.getElementById('carls-count');
      var totalEl = document.getElementById('carls-total');
      var badgeEl = document.getElementById('carls-price-badge');
      var pricePerPerson = 285;

      function updateCarls() {
        var guests = parseInt(slider.value, 10);
        var total = guests * pricePerPerson;
        countEl.textContent = guests;
        totalEl.innerHTML = 'Total: <strong>$' + total.toLocaleString() + '</strong> for ' + guests + ' guests at $285/person';
        badgeEl.innerHTML = '$' + total.toLocaleString() + ' for ' + guests + ' Guests<span class="per-person">$285 per person</span>';
      }

      if (slider) {
        slider.addEventListener('input', updateCarls);
      }

      // Toggle add-ons sections
      document.querySelectorAll('[data-target]').forEach(function (btn) {
        btn.addEventListener('click', function () {
          var target = document.getElementById(btn.getAttribute('data-target'));
          if (target) {
            target.classList.toggle('visible');
            btn.classList.toggle('active');
          }
        });
      });

      // Toggle virtual tour iframes (lazy-load on first click)
      document.querySelectorAll('[data-tour]').forEach(function (btn) {
        btn.addEventListener('click', function () {
          var frame = document.getElementById(btn.getAttribute('data-tour'));
          if (!frame) return;

          if (!frame.querySelector('iframe')) {
            var src = frame.getAttribute('data-src');
            var wrapper = frame.querySelector('.iframe-wrapper');
            if (src && wrapper) {
              var iframe = document.createElement('iframe');
              iframe.src = src;
              iframe.setAttribute('allowfullscreen', '');
              iframe.setAttribute('loading', 'lazy');
              wrapper.appendChild(iframe);
            }
          }

          frame.classList.toggle('visible');
          btn.classList.toggle('active');
        });
      });
    });
  </script>

</body>
</html>
